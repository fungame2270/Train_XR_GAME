using UnityEngine;

public class SemaphorColliderStatus : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public Semaphor semaphore;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
