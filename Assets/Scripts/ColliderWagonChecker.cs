using UnityEngine;

public class ColliderWagonChecker : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    public GameObject wagon;
    void Start()
    {
        
    }
}
